package com.harshul.test

fun main() {


    /*checkFunction(message = "Hello") {
        2
    }

    checkFunctionString(message = "hi") {
        print(it)
        31
    }

    val myList = listOf("hello", "this", "is", "testing")
    myList.forEach {
        println(it)
    }

    val mutableList = mutableListOf("mutable", "list")
    mutableList.add(1, "is")
    mutableList.add("of")
    for (i in mutableList) print("$i ")*/



   /* fun calculate(first: Int, second: Int, message: String) {
        for (i in first..second) println("$i $message")
    }

    fun add(a: Int, b: Int): Int {
        return a + b
    }

    val sum: (Int, Int) -> Int = { a, b -> a + b }

    val itCase: (Int) -> Int = { it * 2 }


    fun checkFunction(message: String, functionAsParameter: () -> Int) {
        println("$message function value is: ${functionAsParameter()}")
    }

    fun checkFunctionString(message: String, functionAsParameter: (String) -> Int) {
        println(" $message function value is: ${functionAsParameter("Hey!")}")
    }*/
}